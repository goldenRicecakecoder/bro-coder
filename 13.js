// method chaining = calling one method after another in one continous line of code

let userName = "bro";

let letter = userName.charAt(0).toUpperCase;

//letter = letter.toUpperCase(); instead of doign this seperatly do the above code, method chaining

console.log(letter);